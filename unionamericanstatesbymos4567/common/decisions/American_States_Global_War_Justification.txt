American_States_Global_War = {
	American_States_Global_War_Justification = {
		icon = GFX_decision_generic_army_support
		allowed = {
			OR = {
				original_tag = UMI
				original_tag = UCA
				original_tag = UAL
				original_tag = UTX
			}
		}
		target_root_trigger = {
			has_variable = var_American_States
			NOT = {
				any_country = { has_variable = var_American_Homeland_Victory_Flag_Won }
			}
			OR = {
				tag = UMI
				tag = UCA
				tag = UAL
				tag = UTX
			}
		}
		targets = { UMI UCA UAL UTX }
		target_trigger = {
			FROM = {
				exists = yes
				NOT = { tag = ROOT }
				OR = {
					tag = UMI
					tag = UCA
					tag = UAL
					tag = UTX
				}
				has_variable = var_American_States_state_on_the_warpath
				has_variable = var_American_States
				NOT = { has_government = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_together_with = ROOT }
				NOT = { is_subject_of = ROOT }
				NOT = { has_war_with = ROOT }
			}
			ROOT = {
				NOT = { is_subject_of = FROM }
			}
		}
		days_remove = 1
		days_re_enable = 30

		ai_will_do = {
			factor = 1
		modifier = {
        		add = 1
			FROM={has_war = yes}
		}
		modifier = {
        		add = 1
			ROOT ={has_annex_war_goal = FROM}
		}
		modifier = {
        		add = 1
			ROOT ={has_border_war_with = FROM}
		}
		modifier = {
        		add = 1
			ROOT ={has_wargoal_against = FROM}
		}
		modifier = {
        		add = 1
			FROM ={has_wargoal_against = ROOT }
		}
		modifier = {
        		add = 1
			ROOT ={is_justifying_wargoal_against = FROM}
		}	
		modifier = {
        		add = 1
			FROM={is_justifying_wargoal_against = ROOT}
		}
		modifier = {
        		add = 1
			ROOT ={has_war_support > 0.50}
		}
		modifier = {
        		add = 1
			FROM ={has_war_support < 0.50}
		}
		modifier = {
        		add = 1
			FROM ={has_political_power < 10}
		}
		}

		cost = 1

		complete_effect = {
			FROM = {
				declare_war_on = { target = ROOT type = annex_everything }
			}
		}
	}
}
